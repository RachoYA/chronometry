# Telegram Web App интеграция

## Что такое Telegram Web App?

Telegram Web App - это возможность запускать веб-приложения прямо внутри Telegram мессенджера. Пользователи могут работать с вашим PWA приложением без выхода из Telegram!

## Преимущества интеграции

✅ **Удобство** - приложение открывается одной кнопкой в Telegram
✅ **Оффлайн работа** - благодаря Service Worker PWA работает без интернета
✅ **Автоматическая авторизация** - используется Telegram ID пользователя
✅ **Нативный опыт** - вибрации, уведомления, интеграция с Telegram UI
✅ **Кроссплатформенность** - работает на iOS, Android и Desktop

## Текущий статус

### ✅ Готово:
1. Создан специальный файл [telegram-app.html](public/telegram-app.html) для Telegram Web App
2. Интегрирован Telegram Web App SDK
3. Адаптирован дизайн под темы Telegram
4. Добавлена автоматическая авторизация через Telegram ID
5. Интегрированы вибрации и Telegram UI элементы
6. Все PWA функции работают (включая оффлайн режим)

### ⚠️ Требование: HTTPS

**Важно:** Telegram Web App работает ТОЛЬКО с HTTPS URL!

```
❌ http://172.16.131.150:5000/telegram-app.html - НЕ работает
✅ https://yourdomain.com/telegram-app.html - РАБОТАЕТ
```

## Варианты развертывания

### Вариант 1: Локальное тестирование с ngrok (быстро)

1. Установите ngrok:
```bash
brew install ngrok
# или
npm install -g ngrok
```

2. Запустите ngrok туннель:
```bash
ngrok http 5000
```

3. Скопируйте HTTPS URL (например: `https://abc123.ngrok.io`)

4. Обновите код бота ([src/bot.js:62](src/bot.js#L62)):
```javascript
keyboard.inline_keyboard.push([{
  text: '📱 Открыть приложение (оффлайн)',
  web_app: { url: 'https://abc123.ngrok.io/telegram-app.html' }
}]);
```

5. Перезапустите бота:
```bash
pkill -f "node.*bot.js"
npm start
```

**Плюсы:**
- Быстро настраивается (5 минут)
- Бесплатно
- Подходит для тестирования

**Минусы:**
- URL меняется при каждом запуске ngrok
- Нужно обновлять код бота каждый раз
- Не подходит для продакшена

### Вариант 2: Развертывание на VPS с SSL

#### A. Использование Railway / Render / Heroku (проще)

1. **Railway.app** (рекомендуется):
```bash
# Установите Railway CLI
npm install -g @railway/cli

# Войдите в аккаунт
railway login

# Инициализируйте проект
railway init

# Разверните
railway up
```

Railway автоматически даст вам HTTPS URL: `https://your-app.up.railway.app`

2. **Render.com**:
- Подключите GitHub репозиторий
- Выберите "Web Service"
- Укажите команду запуска: `npm run web`
- Render автоматически создаст HTTPS URL

3. **Heroku**:
```bash
heroku create your-app-name
git push heroku main
```

#### B. Собственный VPS с Nginx + Let's Encrypt

1. Купите домен (например, на namecheap.com)

2. Настройте DNS A-запись на IP вашего сервера

3. Установите Nginx и Certbot на сервере:
```bash
sudo apt update
sudo apt install nginx certbot python3-certbot-nginx
```

4. Создайте конфигурацию Nginx:
```nginx
server {
    listen 80;
    server_name yourdomain.com;

    location / {
        proxy_pass http://localhost:5000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
```

5. Получите SSL сертификат:
```bash
sudo certbot --nginx -d yourdomain.com
```

6. Certbot автоматически настроит HTTPS!

### Вариант 3: Cloudflare Tunnel (бесплатно)

1. Установите cloudflared:
```bash
brew install cloudflared
```

2. Аутентифицируйтесь:
```bash
cloudflared tunnel login
```

3. Создайте туннель:
```bash
cloudflared tunnel create chronometry
```

4. Настройте маршрутизацию:
```bash
cloudflared tunnel route dns chronometry chronometry.yourdomain.com
```

5. Запустите туннель:
```bash
cloudflared tunnel run chronometry
```

Cloudflare даст вам постоянный HTTPS URL!

## Настройка бота после получения HTTPS URL

1. Откройте [src/bot.js](src/bot.js)

2. Найдите строку 60-63 (функция `getMainMenuKeyboard`)

3. Замените URL на ваш HTTPS URL:
```javascript
keyboard.inline_keyboard.push([{
  text: '📱 Открыть приложение (оффлайн)',
  web_app: { url: 'https://ВАШ_ДОМЕН/telegram-app.html' }
}]);
```

4. Сохраните и перезапустите бота:
```bash
pkill -f "node.*bot.js"
npm start
```

## Тестирование

1. Откройте бота в Telegram
2. Нажмите кнопку "📱 Открыть приложение (оффлайн)"
3. Приложение откроется внутри Telegram
4. Включите авиарежим на телефоне
5. Убедитесь, что приложение продолжает работать!

## Особенности Telegram Web App версии

### Автоматическая авторизация
```javascript
const tgUser = Telegram.WebApp.initDataUnsafe?.user;
if (tgUser) {
    const userName = tgUser.first_name + (tgUser.last_name ? ' ' + tgUser.last_name : '');
    // Автоматически подставляется в поле входа
}
```

### Кнопка "Назад" Telegram
```javascript
Telegram.WebApp.BackButton.onClick(() => {
    // Навигация назад или закрытие приложения
});
```

### Вибрация
```javascript
Telegram.WebApp.HapticFeedback.impactOccurred('medium');
```

### Адаптация к теме Telegram
```css
body {
    background-color: var(--tg-theme-bg-color, #ffffff);
    color: var(--tg-theme-text-color, #000000);
}

.btn-primary {
    background: var(--tg-theme-button-color, #3390ec);
    color: var(--tg-theme-button-text-color, #ffffff);
}
```

### Уведомления через Telegram
```javascript
Telegram.WebApp.showAlert('Работа завершена!');
Telegram.WebApp.showConfirm('Вы уверены?', (confirmed) => {
    if (confirmed) {
        // действие
    }
});
```

## Оффлайн режим в Telegram Web App

### Как это работает:

1. При первом открытии Service Worker кэширует все ресурсы
2. IndexedDB сохраняет данные приложения локально
3. Пользователь может **закрыть Telegram, выключить интернет**
4. При повторном открытии - **всё работает!**

### Что работает оффлайн:

✅ Выбор процесса
✅ Начало работы (таймер запускается)
✅ Прикрепление фото
✅ Завершение работы с комментарием
✅ Просмотр истории
✅ Просмотр статистики

### Что требует интернета:

❌ Синхронизация с сервером (происходит автоматически когда интернет появляется)
❌ Загрузка новых процессов от админа

## Сравнение: Telegram Bot vs Telegram Web App

| Функция | Telegram Bot | Telegram Web App |
|---------|--------------|------------------|
| Скорость работы | Средняя (запросы к боту) | Мгновенная (локально) |
| Интерфейс | Кнопки + текст | Полноценный UI |
| Оффлайн режим | ❌ Нет | ✅ Да (Service Worker) |
| Работа с фото | Загрузка фото в чат | Выбор из галереи |
| Навигация | По кнопкам | Как в приложении |
| Установка | Не требуется | Не требуется |
| Автообновление | Автоматически | Автоматически (PWA) |

## Рекомендация

**Для продакшена используйте оба варианта:**

1. **Telegram Bot** - для быстрых действий, уведомлений
2. **Telegram Web App** - для полноценной работы с хронометражем

Пользователи смогут выбирать удобный им способ!

## Чек-лист развертывания

- [ ] Получен HTTPS URL (ngrok / VPS / Railway / Cloudflare)
- [ ] Обновлен URL в [src/bot.js:62](src/bot.js#L62)
- [ ] Перезапущен бот
- [ ] Протестирована кнопка "📱 Открыть приложение"
- [ ] Проверен оффлайн режим (авиарежим)
- [ ] Проверена автоматическая синхронизация при появлении интернета

## Поддержка

При возникновении проблем:

1. Проверьте, что URL начинается с `https://`
2. Проверьте, что Service Worker активирован (DevTools → Application → Service Workers)
3. Проверьте логи бота: `tail -f bot.log`
4. Проверьте логи сервера: `lsof -ti:5000`

---

**Дата создания:** 25 ноября 2025
**Статус:** Готово к развертыванию (требуется HTTPS)
