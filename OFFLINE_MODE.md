# Как работает оффлайн режим

## Архитектура системы

```
┌─────────────────────────────────────────────────────────────┐
│                    TELEGRAM ПОЛЬЗОВАТЕЛЬ                     │
│         (Нужен интернет только для связи с Telegram)         │
└───────────────────────────┬─────────────────────────────────┘
                            │
                            │ Интернет
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                    TELEGRAM API СЕРВЕР                       │
│           (Облачные серверы Telegram)                        │
└───────────────────────────┬─────────────────────────────────┘
                            │
                            │ Polling/WebHook
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                    ВАШ СЕРВЕР (Node.js)                      │
│                                                               │
│  ┌────────────────────────────────────────────────────┐     │
│  │  bot.js - Telegram Bot                             │     │
│  │  • Получает сообщения                              │     │
│  │  • Обрабатывает команды                            │     │
│  │  • Отправляет ответы                               │     │
│  └──────────────────┬─────────────────────────────────┘     │
│                     │                                         │
│                     ▼                                         │
│  ┌────────────────────────────────────────────────────┐     │
│  │  database.js - Модуль БД                           │     │
│  │  • Создание записей                                │     │
│  │  • Чтение данных                                   │     │
│  │  • Обновление записей                              │     │
│  └──────────────────┬─────────────────────────────────┘     │
│                     │                                         │
│                     ▼                                         │
│  ┌────────────────────────────────────────────────────┐     │
│  │  SQLite Database (chronometry.db)                  │     │
│  │  • Локальный файл на диске                         │     │
│  │  • Все данные хранятся здесь                       │     │
│  │  • Не требует интернета                            │     │
│  └────────────────────────────────────────────────────┘     │
│                                                               │
│  ┌────────────────────────────────────────────────────┐     │
│  │  photos/ - Папка с фотографиями                    │     │
│  │  • Файлы сохраняются на диске                      │     │
│  │  • Локальное хранилище                             │     │
│  └────────────────────────────────────────────────────┘     │
│                                                               │
└─────────────────────────────────────────────────────────────┘
```

## Как это работает

### 1. Когда пользователь отправляет команду

```
Пользователь → Telegram → Ваш сервер → SQLite (локально)
   📱             ☁️           💻             💾
```

**Шаг за шагом:**
1. Пользователь нажимает "▶️ Начать работу" в Telegram
2. Сообщение идет через интернет на серверы Telegram
3. Telegram отправляет webhook/polling вашему боту
4. Бот получает команду и **МГНОВЕННО** сохраняет в SQLite
5. SQLite - это файл на диске, не требует интернета
6. Бот отправляет подтверждение обратно в Telegram

### 2. Хранение данных оффлайн

**SQLite - это НЕ облачная БД!**

❌ Не MySQL (требует сервер)
❌ Не PostgreSQL (требует сервер)
❌ Не MongoDB (требует сервер)
✅ SQLite - просто файл на диске!

```bash
/Users/yourname/Downloads/Игра/
├── data/
│   └── chronometry.db  ← Это обычный файл, как .docx или .xlsx
└── photos/
    ├── 1732527894123_photo.jpg
    └── 1732527895456_photo.jpg
```

### 3. Работа с фотографиями

```
Фото отправлено → Telegram API → Скачивание → Локальный диск
     📸              ☁️              ⬇️           💾
```

Когда пользователь отправляет фото:
1. Фото загружается на серверы Telegram
2. Бот получает file_id (ссылку на фото)
3. Бот **скачивает** фото через Telegram API
4. Фото сохраняется в папку `photos/` на диске
5. Путь к файлу сохраняется в SQLite

**Важно:** После скачивания фото хранится локально навсегда!

## Почему это "оффлайн"?

### Что ТРЕБУЕТ интернета:
- ✅ Отправка/получение сообщений в Telegram
- ✅ Скачивание фотографий с серверов Telegram

### Что НЕ требует интернета:
- ❌ Сохранение данных в БД (мгновенно, локально)
- ❌ Чтение данных из БД (мгновенно, локально)
- ❌ Хранение фотографий (уже на диске)
- ❌ Подсчет статистики (работает с локальной Б��)

## Преимущества оффлайн подхода

### 1. Скорость 🚀
```javascript
// Запись в SQLite
await db.startTimeRecord(userId, processId); // ~1-5 мс

// VS облачная БД
await cloudDB.insert(...); // ~50-500 мс (зависит от интернета)
```

### 2. Надежность 💪
- Данные НЕ зависят от интернета
- Если сервер Telegram недоступен, данные уже сохранены
- Нет риска потери данных при проблемах с сетью

### 3. Простота 🎯
- Не нужно настраивать облачную БД
- Не нужно платить за хостинг БД
- Один файл - вся база данных

### 4. Приватность 🔒
- Данные хранятся только у вас
- Никакие облачные сервисы не имеют доступа
- Полный контроль над данными

## Сценарии работы

### Сценарий 1: Стабильный интернет
```
1. Пользователь: "Начать работу"
   → Telegram → Бот → SQLite (сохранено за 2ms)

2. Бот отвечает: "✅ Работа начата"
   → SQLite → Бот → Telegram → Пользователь

Время: ~100-300ms (в основном на связь с Telegram)
```

### Сценарий 2: Медленный интернет
```
1. Пользователь: "Начать работу" (отправка 5 секунд)
   → [медленно] → Telegram → Бот → SQLite (сохранено за 2ms)

2. Данные УЖЕ в БД, пользователь еще ждет ответа

3. Бот отвечает через медленный интернет (еще 5 секунд)

Важно: Данные сохранены надежно, даже если ответ идет долго!
```

### Сценарий 3: Интернет пропал ПОСЛЕ отправки
```
1. Пользователь: "Начать работу" ✅ (отправлено)
2. Telegram → Бот ✅
3. SQLite сохранил данные ✅
4. 💥 Интернет пропал
5. Бот НЕ может ответить ❌
6. НО! Данные УЖЕ в БД ✅

Когда интернет вернется:
- Данные останутся в БД
- Пользователь может проверить через /status
```

## Структура базы данных

### Таблица: time_records
```sql
CREATE TABLE time_records (
  id INTEGER PRIMARY KEY,
  user_id INTEGER,
  process_id INTEGER,
  start_time DATETIME,      -- Записано локально
  end_time DATETIME,         -- Записано локально
  duration INTEGER,          -- Вычислено локально
  comment TEXT,              -- Хранится локально
  synced INTEGER DEFAULT 0  -- Флаг синхронизации (пока не используется)
)
```

Поле `synced` добавлено для будущего расширения:
- Можно добавить синхронизацию с облаком
- Можно экспортировать данные
- Можно делать резервные копии

## Расширение возможностей

### 1. Добавить облачную синхронизацию

```javascript
// После сохранения в SQLite
await db.startTimeRecord(userId, processId);

// Опционально: отправить в облако
try {
  await cloudSync.send(record);
  await db.markAsSynced(recordId);
} catch (error) {
  // Не страшно, данные в SQLite останутся
}
```

### 2. Экспорт данных

```javascript
// Экспорт в Excel
const records = await db.getAllRecords();
const excel = generateExcel(records);
bot.sendDocument(chatId, excel);
```

### 3. Резервное копирование

```bash
# Простое копирование файла
cp data/chronometry.db backup/chronometry_2025-11-25.db

# Или автоматически через cron
0 2 * * * cp /path/to/chronometry.db /path/to/backup/
```

## Технические детали

### SQLite характеристики:
- **Размер:** ~500 KB на диске (пустая БД)
- **Скорость записи:** ~50,000 операций/сек
- **Скорость чтения:** ~100,000 операций/сек
- **Максимальный размер:** До 281 терабайт
- **Concurrent connections:** Множественное чтение, одна запись

### Фотографии:
- **Формат:** JPG (как из Telegram)
- **Средний размер:** 200-500 KB на фото
- **Хранение:** Файловая система
- **Именование:** timestamp_originalname.jpg

### Производительность:
```javascript
// Типичные операции и их время:
db.startTimeRecord()     // ~2-5 ms
db.getActiveRecord()     // ~1-3 ms
db.stopTimeRecord()      // ~3-7 ms
db.getRecordStats()      // ~10-20 ms (с JOIN)
db.addPhoto()            // ~2-5 ms
Photo download           // ~500-2000 ms (зависит от интернета)
```

## Сравнение с облачными решениями

### Локальный SQLite (текущее решение)
✅ Мгновенное сохранение (2-5 мс)
✅ Не зависит от интернета
✅ Бесплатно
✅ Полный контроль
❌ Нужен сервер для бота
❌ Нет автоматической синхронизации между устройствами

### Firebase / Supabase (облачное)
✅ Автосинхронизация
✅ Доступ с любого устройства
❌ Медленнее (50-500 мс)
❌ Требует интернет
❌ Платные тарифы
❌ Данные у третьей стороны

### Гибридный подход (лучшее из обоих)
✅ SQLite для быстрого сохранения
✅ Фоновая синхронизация в облако
✅ Работает даже без интернета
✅ Резервная копия в облаке

## FAQ

**Q: Что если сервер выключится?**
A: База данных - это файл. При запуске все данные снова доступны.

**Q: Потеряются ли данные при обновлении бота?**
A: Нет! База данных - отдельный файл, не связан с кодом.

**Q: Можно ли работать с телефона без интернета?**
A: Telegram требует интернет для отправки сообщений. Но данные на сервере хранятся оффлайн.

**Q: Как сделать резервную копию?**
A: Просто скопируйте файлы `data/chronometry.db` и папку `photos/`

**Q: Можно ли подключить несколько ботов к одной БД?**
A: Да, SQLite поддерживает множественное чтение. Но для множественной записи лучше использовать очередь.

**Q: Что значит "оффлайн" в контексте Telegram бота?**
A: Данные сохраняются локально, не в облаке. Но для работы Telegram нужен интернет.

## Заключение

"Оффлайн режим" означает:
1. ✅ Локальное хранение данных (SQLite)
2. ✅ Мгновенное сохранение без задержек
3. ✅ Независимость от облачных сервисов
4. ✅ Полный контроль ��ад данными
5. ✅ Высокая скорость работы

Но для работы через Telegram всё равно нужен интернет для связи с API Telegram.

**Итог:** Данные хранятся оффлайн, работа идет онлайн.